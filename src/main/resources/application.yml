server:
  port: 9000

spring:
  profiles:
    active: dev
  application:
    name: Gamygyn
  thymeleaf:
    enabled: true
    prefix: classpath:/templates/
    suffix: .html
    cache: false
  devtools:
    livereload:
      enabled: true
    restart:
      enabled: true